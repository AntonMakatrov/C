name: lab1

on:
  push:
    branches: [ lab1 ]
  pull_request:
    branches: [ lab1 ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: |
        cd lab1/
        make lab1_test
        ./lab1_test
    - name: make TEST
      run: cd make TEST
    - name: lab1
      run: ./lab1
    - name: lab1_test
      run: ./lab1_test
