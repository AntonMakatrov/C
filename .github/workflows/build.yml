name: labs

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build5
      run: |
        cd lab5
        make task_test
        ./task_test
    - name: Build6
      run: |
        cd lab6
        make maintest
        ./maintest
